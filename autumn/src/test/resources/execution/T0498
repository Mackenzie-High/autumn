module Main in execution;

@Start
defun main (args : String[]) : void
{
    val list = ["X", "Y", "Z"];

    assert F::get(list, 0) == "X";
    assert F::get(list, 1) == "Y";
    assert F::get(list, 2) == "Z";
}