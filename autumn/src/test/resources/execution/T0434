module Main in execution;

@Start
defun main (args : String[]) : void
{
    val list = ["A", "B", "C", "D", "E"];

    val result = F::enumerate(list);

    assert result == [[0, "A"], [1, "B"], [2, "C"], [3, "D"], [4, "E"]];
}