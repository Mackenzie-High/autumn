module Main in execution;

@Start
defun main (args : String[]) : void
{
    val field = F::findField((class Integer), "MAX_VALUE");

    assert field != null;

    assert field.getName() == "MAX_VALUE";
}