## Summary

This function compares two values and then returns the result.

## Signature

compare (left : [Comparable](https://docs.oracle.com/javase/7/docs/api/java/lang/Comparable.html), right : [Comparable](https://docs.oracle.com/javase/7/docs/api/java/lang/Comparable.html)) : int

## Formals

+ Parameter leftis the left operand.
+ Parameter rightis the right operand.

## Returns

The result is negative when (<i>left</i> < <i>right</i>), zero when (<i>left</i> == <i>right</i>), and positive when (<i>left</i> > <i>right</i>).

## Details

+ A null value is considered to be less-than a non-null value.

## Example

**Source Code:**

```plain
module Main in execution;%0A%0A@Start%0Adefun main (args : String[]) : void%0A{%0A    F::println( F::compare(2, 3) );%0A    F::println( F::compare(3, 3) );%0A    F::println( F::compare(3, 2) );%0A    F::println();%0A%0A    F::println( F::compare(null,    0) );%0A    F::println( F::compare(null, null) );%0A    F::println( F::compare(   0, null) );%0A    F::println();%0A}
```

**Output:**

```plain
-1%0A0%0A1%0A%0A-1%0A0%0A1
```

