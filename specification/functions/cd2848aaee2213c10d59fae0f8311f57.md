## Summary

This function transverses an iterable in order to find the minimum value therein.

## Signature

minimum (values : [Iterable](https://docs.oracle.com/javase/7/docs/api/java/lang/Iterable.html)) : [Object](https://docs.oracle.com/javase/7/docs/api/java/lang/Object.html)

## Formals

+ Parameter valuesis the iterable to transverse.

## Returns

Return the minimum value found in the iterable.

## Throws

+ [NullPointerException](https://docs.oracle.com/javase/7/docs/api/java/lang/NullPointerException.html), if <i>values</i> is null.

## Example 1

**Source Code:**

```plain
module Main in execution;%0A%0A@Start%0Adefun main (args : String[]) : void%0A{%0A    # Create some data.%0A    val ages = [19, 18, 20, 24, 25, 23, 21, 22];%0A%0A    # Compute the minimum, maximum, and average.%0A    val maximum = F::maximum(ages);%0A    val minimum = F::minimum(ages);%0A    val average = F::average(ages);%0A%0A    # Print the results.%0A    F::println(%22Maximum = %22 .. maximum);%0A    F::println(%22Minimum = %22 .. minimum);%0A    F::println(%22Average = %22 .. average.stripTrailingZeros());%0A}
```

**Output:**

```plain
Maximum = 25%0AMinimum = 18%0AAverage = 21.5
```

## Example 2

**Source Code:**

```plain
module Main in execution;%0A%0A@Start%0Adefun main (args : String[]) : void%0A{%0A    val values = [301, 401, 500, 402, 302];%0A%0A    val first = F::first(values);%0A    val last = F::last(values);%0A    val min = F::minimum(values);%0A    val max = F::maximum(values);%0A%0A    F::println(%22First = %22 .. first);%0A    F::println(%22Last = %22 .. last);%0A    F::println(%22Min = %22 .. min);%0A    F::println(%22Max = %22 .. max);%0A}
```

**Output:**

```plain
First = 301%0ALast = 302%0AMin = 301%0AMax = 500
```

