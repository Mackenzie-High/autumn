module Main in execution;

@Start
defun main (args : String[]) : void
{
    val x = (call Main::G);

    assert x == 123;

    assert (instanceof x : Integer);
}

defun G() : Object
{
    val x = 123;

    assert (class int) == ((locals).typeOf "x");

    yield x;
}