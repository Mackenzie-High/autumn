module Main in execution;

@Start
defun main (args : String[]) : void
{
    val map = new TreeMap();

    map.put("A", "X");
    map.put("B", "Y");
    map.put("C", "Z");

    val result = F::iter(map);

    assert result.size() == 3;
    assert F::newList(result) == ["A", "B", "C"];

    var i = 0;

    foreach (value : Object in result)
    {
        assert value == F::newList(result).get(i);
        assert map.containsKey(value);

        i = i + 1;
    }
}