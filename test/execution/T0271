module Main in execution;

tuple Person (name : String);

@Start
defun main (args : String[]) : void
{
    var p = new Person("Molly");

    assert p.isTuple() == true;

    assert p.isStruct() == false;
}