module Main in execution;

@Start
defun main (args : String[]) : void
{
    assert (class void).getName() == "void";

    assert (class int).getName() == "int";

    assert (class String).getName() == "java.lang.String";

    assert (class String[][][]).getName() == "[[[Ljava.lang.String;";
}