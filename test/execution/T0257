module Main in execution;

tuple Person (name : String, age : int);


@Start
defun main (args : String[]) : void
{
    var p = new Person("Avril", 30);

    val k = new BitSet

    assert p.hashCode() == F::bitwiseXor(p.keys().hashCode(), p.values().hashCode());
}