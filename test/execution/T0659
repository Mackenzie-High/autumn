module Main in execution;

@Start
defun main (args : String[]) : void
{
    try
    {
        lambda p : Ordering => 0;

        F::sort(null, p);

        assert false;
    }
    catch(ex : NullPointerException)
    {
        nop;
    }
}