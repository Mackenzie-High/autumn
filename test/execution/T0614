module Main in execution;

@Start
defun main (args : String[]) : void
{
    val original = new LinkedList(["A", "B", "C"]);

    val copy = F::newList(original is Iterable);

    original.add("X");

    assert original == ["A", "B", "C", "X"];

    assert copy == ["A", "B", "C"];
}