module Main in execution;

@Start
defun main (args : String[]) : void
{
    var s = "";

    var modifier = 0 is Integer;

    for (i = 0; i < 5; modifier)
    {
        s = s .. i;

        modifier = i + 1;
    }

    assert s == "01234";
}