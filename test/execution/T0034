module Main in execution;

@Start
defun main (args : String[]) : void
{
    val input = [];

    var i = 1;

    foreach(x : Integer in input)
    {
        assert false;

        let i = i + 1;
    }

    assert i == 1;
}

