module Main in execution;

functor Moo (x : int, y : int) : int;

@Start
defun main (args : String[]) : void
{
    val x = 17;

    lambda p : Moo => $1 + $2 + x;

    assert p.invoke(100, 3) == 120;
}