module Main in execution;

annotation City;

@Start
defun main (args : String[]) : void
{
    val anno = F::findAnnotation(class City, class autumn.lang.internals.annotations.AnnotationDefinition);

    assert anno != null;
}